name: Build and Push ARM Docker Image

on:
  workflow_dispatch:
  schedule:
    - cron: '19 7 4/7 * *'  # 定时每小时运行一次

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Clone repository
      run: git clone https://github.com/14790897/auto-read-liunxdo repo

    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          
    - name: Build and push
      id: docker_build
      uses: docker/build-push-action@v2
      with:
        context: ./repo  # 这里是您clone下来的存储库路径
        push: true
        tags: cym31152x/gpt4free:latest
        platforms: linux/arm/v8,linux/amd64,linux/386,linux/arm/v7,linux/arm64,windows/amd64,windows/arm64
